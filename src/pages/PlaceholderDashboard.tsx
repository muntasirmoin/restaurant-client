export const PlaceholderDashboard = ({ title }: { title: string }) => (
  <div className="p-8">
    <h1 className="text-2xl font-semibold">{title}</h1>
    <p className="text-muted-foreground mt-2">Dashboard coming soon.</p>
  </div>
);
